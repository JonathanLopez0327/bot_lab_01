# Entorno virtual
.venv/
venv/

# Python
__pycache__/
*.pyc
*.pyo

# Django (BD local; solo la usa el test runner)
db.sqlite3

# Secretos / entorno local — NUNCA subir (contiene API keys)
.env
.env.local

# SO / editor
.DS_Store
